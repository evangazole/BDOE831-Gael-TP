Atelier Big Data Traitement

Suivi et consigne atelier

Contexte

Découper le projet en 3 étapes :

    Conception de l'architecture
    Implémentation de l'architecture
    Utilisation de l'architecture

La conception de l'architecture doit permettre doit permettre au besoin principal : détecter une tentative d'intrusion sur la base de logs de différents formats.

Le modèle principalement attendu doit s'appuyer sur le fichier Network_logs.csv, c'est à dire sans aucune information de temps. Les autres modèles sont en bonus.

 
Livrables

Livrables dans un dépôt GIT et/ou partage par exemple des accès Fabric à votre espace de travail avec mon compte

Dépôt GIT à transmettre via un Google Form. Déposer dans le dépôt GIT tous les fichiers de configuration (docker-compose, Dockerfile, script d'installation, script SQL, ...) pour que je sois autonome lors de l'évaluation.
Modalités de travail

Le travail est individuel et le rendu également. L'entraide est toujours possible.

Points réguliers pour illustrer un cas pratique toutes les 40 min ~.
Sources de données

Pour récupérer les fichiers de données à exploiter, il faut consulter le partage Google Drive Fichier de données à traiter

Pour traiter la caractéristique temps, voir Feature time-related.

Identifier quelles sont les variables aléatoires discrètes et continues.

Identifier quelles sont les caractéristiques/features et quelles sont les étiquettes/labels/valeurs cible, dans le cas d'une apprentissage supervisé.

Dans le fichier Network_logs.csv, certaines informations sont inutiles ou pas exploitables directement :

    les adresses IPs devront être remplacées par les codes pays, en s'appuyant sur la base de données fournie également dans le partage
    les types correspondent-ils aux natures des variables aléatoires
    est-ce que toutes les colonnes sont utiles